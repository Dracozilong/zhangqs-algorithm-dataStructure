package zhangqs.sort.inserSort;

/**
 *  插入排序
 */
public class InsertSort {

    // 思路分析
     // 1. 从第一个元素开始，假设该元素已经被排序，获取第二个元素，如果第二个元素比第一个元素小 ，交换排序
     // 2. 依次获取 3 -> n-1数组长度的值，与之前已经排序的 1 ->n-2 数组中的元素进行排序,获取最小值
}
